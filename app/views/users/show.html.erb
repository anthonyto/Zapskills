<% @title="Profile"%>
<div class="container target">
	<div class="row">
		<div class="col-sm-10">
			<h1><%= current_user.first_name %></h1>
			<br>
		</div>
		<div class="col-sm-2">
			<%= image_tag current_user.avatar.url, class: "img-thumbnail pull-left" %>
		</div>
	</div>
	<br>
</div>

<div class="row">
	<div class="col-sm-3">
		<!--left col-->
		<ul class="list-group">
			<li class="list-group-item text-muted" contenteditable="false">Profile</li>
			<li class="list-group-item text-right">
				<strong class="pull-left">Location</strong><%= current_user.city %>
			</li>
			<li class="list-group-item text-right">
				<strong class="pull-left">Email </strong><%= current_user.email %>
			</li>
			<li class="list-group-item text-right">
				<strong class="pull-left">Rating </strong><img src="images/stars_4.png" alt="4 Stars" width="100" height="25">
			</li>
			<% if @user == current_user %>
				<li class="list-group-item text-right"><span class="pull-left">
					<strong><%= link_to "Edit", edit_user_path(current_user) %></strong>
				</li>
			<% end %>
		</ul>

	</div>
	<div class="col-sm-8">
		<div class="panel panel-default">
		<div class="panel-heading">Tutoring</div>
			<div class="panel-body"> 
				<div class="table-fixed">
					<table class="table table-striped table-hover">
						<thead>
							<tr>
								<th>Subject</th>
								<th>Experience</th>
							</tr>
						</thead>
						<tbody>
							<% current_user.experiences.each do |experience| %>
								<tr>
									<td><%= experience.skill.name %></td>
									<td><%= experience.level %></td>
								</tr>
							<% end %>
						</tbody>
					</table>
					<% if @user == current_user %>
						<%= link_to "Add Skill", new_user_experience_path(current_user) %>
					<% end %>
				</div>
			</div>
		</div>
	</div>
</div>